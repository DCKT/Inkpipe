services:
  inkpipe:
    image: ghcr.io/dckt/inkpipe:latest
    ports:
      - "3000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - inkpipe-temp:/tmp/inkpipe

volumes:
  inkpipe-temp:
